# Backlog — детальный список задач

Приоритеты: **P0** (критично), **P1** (важно), **P2** (желательно)

**Важно:** 
- Архитектура CTA зафиксирована в [`cta.md`](cta.md) — это основной документ по гибридному подходу и бизнес-правилам.
- **Разделение P0/P1:** P0 — минимальный MVP с простой логикой CTA, P1 — расширенная функциональность (отложенные CTA, все типы, сложные пороги). Это позволяет реализовать рабочий вариант быстрее, не теряя архитектурной целостности.

---

## Этап 1: MVP — Основной функционал

### P0: Сбор контактов (имя + телефон)
**Файлы:** `widget.js`
**Описание:** 
- Добавить форму сбора имени и телефона
- Показывать форму по сигналу от бота или после N сообщений
- Сохранять данные в состоянии виджета

**Детали:**
- Создать функцию `showContactForm()`
- Добавить поля `#botWidgetName`, `#botWidgetPhone`
- Интегрировать с логикой CTA (см. ниже)

**Зависимости:** Нет

---

### P0: Валидация данных
**Файлы:** `widget.js`
**Описание:**
- Проверка имени на "человеческое" (минимум 2 символа, кириллица/латиница)
- Маска телефона (российский формат: +7 (XXX) XXX-XX-XX)
- Визуальная обратная связь при ошибках

**Детали:**
- Функция `validateName(name)` — проверка на минимальную длину и допустимые символы
- Функция `formatPhone(phone)` — форматирование и валидация
- Использовать библиотеку или написать регулярное выражение для телефона

**Зависимости:** P0: Сбор контактов

---

### P0: Отправка заявок на почту
**Файлы:** Нужен новый backend-файл (PHP или Node.js endpoint)
**Описание:**
- Создать API endpoint для отправки email
- Принимать JSON: `{ name, phone, message?, timestamp }`
- Отправлять на почту клиники
- Возвращать статус успеха/ошибки

**Детали:**
- Вариант 1: PHP скрипт `/api/send-lead.php` на VPS
- Вариант 2: Node.js endpoint в отдельном сервисе
- Использовать SMTP или mail() функцию
- Логировать отправки

**Зависимости:** P0: Сбор контактов, P0: Валидация данных

**Вопрос:** Какой backend предпочтительнее? PHP проще для nginx, Node.js гибче.

---

### P0: Подтверждение отправки заявки
**Файлы:** `widget.js`
**Описание:**
- После успешной отправки показывать сообщение бота
- Очищать форму
- Возвращать пользователя в диалог

**Детали:**
- Обработка ответа от API отправки
- Показ сообщения типа "Спасибо! Мы свяжемся с вами в ближайшее время"
- Скрытие формы контактов

**Зависимости:** P0: Отправка заявок на почту

---

## Этап 2: UX и дизайн

### P1: Улучшение дизайна виджета
**Файлы:** `widget.css`
**Описание:**
- Современный, чистый дизайн
- Улучшенная типографика
- Плавные анимации открытия/закрытия
- Тени и скругления

**Детали:**
- Переработать цветовую схему
- Добавить hover-эффекты
- Улучшить читаемость сообщений
- Адаптивность под мобильные устройства

**Зависимости:** Нет (можно параллельно с Этапом 1)

---

### P1: Стартовое меню с кнопками
**Файлы:** `widget.js`, `widget.css`
**Описание:**
- При первом открытии показывать приветствие + 2-3 кнопки
- Кнопки: "Посмотреть цены", "Я боюсь боли", "Записаться"
- По клику отправлять соответствующий вопрос в Flowise

**Детали:**
- Функция `showWelcomeMenu()`
- Создать компонент кнопок в HTML
- Стили для кнопок в `widget.css`
- После выбора кнопки — скрывать меню, показывать диалог

**Зависимости:** Нет

**Связано с:** `logic.md` раздел 13

---

### P1: Форматирование ответов (списки, эмодзи)
**Файлы:** `widget.js`
**Описание:**
- Парсить markdown в ответах от Flowise
- Поддерживать списки, жирный текст, эмодзи
- Отображать форматированный текст в сообщениях

**Детали:**
- Функция `formatMessage(text)` — парсинг markdown
- Использовать библиотеку (например, `marked` или простой парсер)
- Обновить функцию `addMsg()` для поддержки HTML

**Зависимости:** Нет

**Связано с:** `logic.md` раздел 12 (использование эмодзи и списков)

---

## Этап 3: Логика и ограничения

### P0: Логика CTA — минимальный MVP
**Файлы:** `widget.js`, Flowise (настройка Structured Output)
**Описание:**
- Базовая реализация гибридного подхода: GPT рекомендует CTA, фронт проверяет правила и решает
- Простая логика без отложенных CTA и сложных confidence-порогов
- Только тип CTA `booking` (запись на консультацию)

**Архитектура (упрощённый поток):**
```
Flowise LLM → Structured Output (JSON) → parseFlowiseResponse() → 
applyBusinessRules() → shouldShowCTA() → renderAnswer() → renderCTA() (или нет)
```

**JSON схема ответа от Flowise (минимальная для P0):**
```json
{
  "answer": "Текст ответа пользователю",
  "ui": {
    "showCTA": true,
    "ctaIntent": "booking"
  },
  "meta": {
    "stage": "ready",
    "confidence": 0.82
  },
  "flags": {
    "emotional": false
  }
}
```

**Детали реализации P0:**

**В Flowise:**
- Настроить LLM ноду с Structured Output (JSON Schema)
- Минимальная схема: `answer`, `ui.showCTA`, `ui.ctaIntent`, `meta.stage`, `meta.confidence`, `flags.emotional`
- Промпт учит модель определять стадию диалога и классифицировать эмоциональное состояние
- `meta.confidence` — уверенность модели в классификации
- **Важно:** Flowise не знает про "продуктовый CTA" — он только классифицирует состояние, фронт решает форму представления

**В виджете (`widget.js`):**
- Функция `parseFlowiseResponse(data)` — парсит JSON ответ
- Функция `applyBusinessRules(response, state)` — проверяет базовые условия
- Функция `shouldShowCTA(ui, meta, flags, state)` — финальное решение о показе кнопки
- Функция `renderAnswer(answer)` — отображает текст ответа
- Функция `renderCTA(ctaIntent)` — отображает кнопку CTA (генерирует текст по `ctaIntent`)
- Состояние виджета: `currentStage`, `dialogState` (без `pendingCTA`, `ctaShownCount` для P0)

**Бизнес-правила на фронте (P0 — минимальный набор):**
- Если `dialogState !== 'normal'` → не показывать CTA
- Если `flags.emotional === true` → только диалоговый CTA в тексте, кнопки не показывать
- Если `meta.stage !== 'ready'` → не показывать CTA
- Если `meta.confidence < 0.4` → игнорировать рекомендации GPT
- Если JSON не распарсился → fallback: показать только текст, без CTA

**Логика отображения (P0):**
```javascript
// Псевдокод логики P0
if (dialogState !== 'normal') return; // нет CTA
if (flags.emotional === true) {
  renderAnswer(answer); // только текст, кнопок нет
  return;
}
if (meta.stage === 'ready' && ui.showCTA === true && meta.confidence >= 0.4) {
  renderCTA('booking'); // показать кнопку "Записаться"
}
```

**Что НЕ входит в P0:**
- Отложенные CTA (`pendingCTA`)
- Сложные confidence-пороги (только один: < 0.4)
- Типы CTA кроме `booking`
- `ctaTiming`, `ctaText` (генерируются фронтом)
- Лимиты показов CTA (`ctaShownCount`)

**Зависимости:** P0: Сбор контактов

**Связано с:** 
- `cta.md` — финальная архитектура CTA (описывает полную реализацию)
- `logic.md` раздел 15 — общие правила CTA

**Документация:** [Flowise Structured Output](https://docs.flowiseai.com/tutorials/structured-output)

---

### P1: Расширенная логика CTA
**Файлы:** `widget.js`, Flowise (расширение Structured Output)
**Описание:**
- Расширение базовой логики: отложенные CTA, все типы CTA, сложные confidence-пороги
- Полная реализация архитектуры из `cta.md`

**Что добавляется к P0:**

**Расширенная JSON схема:**
- Добавить `ui.ctaText`, `ui.ctaTiming` (опционально)
- Добавить `meta.intent`, `meta.shouldHandoff`
- Добавить `flags.isOffTopic`, `flags.isFlood`, `flags.needsEmpathy`

**Все типы CTA:**
- `soft_help` — мягкий шаг
- `info_step` — информационный шаг
- `contact` — предложение оставить контакт
- `booking` — запись (уже в P0)
- `handoff` — передача администратору

**Отложенные CTA:**
- Функция `savePendingCTA(response, state)` — сохраняет рекомендацию GPT
- Функция `checkPendingCTA(state)` — проверяет возможность использования
- Состояние `pendingCTA` в `widgetState`
- Логика использования отложенного CTA при изменении контекста

**Сложные confidence-пороги:**
- 🟢 `confidence >= 0.7` — доверять, но проверять правила
- 🟡 `0.4 <= confidence < 0.7` — консервативная логика, CTA может быть отложен
- 🔴 `confidence < 0.4` — игнорировать рекомендации GPT

**Лимиты показов:**
- Счётчик `ctaShownCount`
- Лимит `MAX_CTA_SHOWS`
- При превышении лимита → сохранить в `pendingCTA`

**Расширенный пайплайн:**
```
Flowise → JSON → parseFlowiseResponse() → validateJSON() → 
applyBusinessRules() → shouldShowCTA() / savePendingCTA() → 
renderAnswer() → renderCTA() (или нет) / checkPendingCTA()
```

**Зависимости:** P0: Логика CTA (минимальный MVP)

**Связано с:** 
- `cta.md` — полная архитектура CTA (основной документ)
- `logic.md` раздел 15 — общие правила CTA

---

### P1: Ограничение флуда/спама
**Файлы:** `widget.js` (возможно, нужна логика в Flowise)
**Описание:**
- Отслеживать нецелевые сообщения
- После 2-3 попыток флуда — завершать диалог
- Показывать финальное сообщение и блокировать ввод

**Детали:**
- Счётчик нецелевых ответов
- Flowise должен возвращать флаг `isOffTopic: true`
- Функция `handleFlood()` — блокировка после N попыток
- Показ сообщения "Извините, я могу помочь только с вопросами о стоматологии"

**Зависимости:** Нет

**Связано с:** `rules.md` раздел 4, `plan.md` последний абзац

---

### P1: Учёт рабочего времени (часовой пояс Камчатки)
**Файлы:** `widget.js`, backend для проверки времени
**Описание:**
- Определять текущее время в часовом поясе Камчатки (UTC+12)
- При отправке заявки вне рабочего времени — показывать соответствующее сообщение
- Рабочее время: уточнить у клиента (например, 9:00-18:00)

**Детали:**
- Функция `isWorkingHours()` — проверка времени
- Использовать библиотеку для работы с часовыми поясами (например, `date-fns-tz`)
- Сообщение: "Сейчас клиника не работает, мы свяжемся с вами в рабочее время"

**Зависимости:** P0: Отправка заявок на почту

**Связано с:** `logic.md` раздел 12

**Вопрос:** Какое рабочее время у клиники? (дни недели, часы)

---

### P2: Настройки ответов Flowise
**Файлы:** Настройки в Flowise UI (не код)
**Описание:**
- Настроить температуру модели (для предсказуемости)
- Ограничить длину ответов
- Настроить другие параметры LLM

**Детали:**
- Это делается в интерфейсе Flowise
- Температура: 0.7-0.8 (баланс между креативностью и стабильностью)
- Max tokens: 300-500 (для краткости)

**Зависимости:** Нет

---

## Этап 4: Интеграции и мониторинг

### P2: Логирование диалогов
**Файлы:** Backend endpoint для логов
**Описание:**
- Сохранять все сообщения пользователя и ответы бота
- Фиксировать метаданные: timestamp, sessionId, тип события

**Детали:**
- Создать endpoint `/api/log` для сохранения логов
- Формат: `{ sessionId, message, role, timestamp, metadata? }`
- Сохранять в файл или БД (SQLite/PostgreSQL)
- Логировать: обращения, попытки записи, ошибки, handoff

**Зависимости:** Нет

**Связано с:** `logic.md` раздел 14

---

### P2: Интеграция с CRM клиники
**Файлы:** Backend интеграция
**Описание:**
- Если у клиники есть CRM — интегрировать отправку лидов
- Альтернатива email-уведомлениям

**Детали:**
- Уточнить, какая CRM используется
- Создать интеграцию через API CRM
- Fallback на email, если CRM недоступна

**Зависимости:** P0: Отправка заявок на почту

**Вопрос:** Использует ли клиника CRM? Какая?

---

### P2: Метрики и аналитика
**Файлы:** Backend endpoint для метрик
**Описание:**
- Собирать метрики: количество обращений, конверсия в заявки, средняя длина диалога
- Простой dashboard или экспорт в CSV

**Детали:**
- Endpoint `/api/metrics` для получения статистики
- Агрегация данных из логов
- Можно использовать готовые решения (Google Analytics события)

**Зависимости:** P2: Логирование диалогов

---

## Технические детали

### Файлы проекта

**Frontend:**
- `widget.js` — основная логика виджета
- `widget.css` — стили виджета

**Backend (нужно создать):**
- `/api/send-lead.php` или `/api/send-lead.js` — отправка заявок
- `/api/log.php` или `/api/log.js` — логирование
- `/api/metrics.php` или `/api/metrics.js` — метрики

**Конфигурация:**
- Настройки Flowise (температура, токены) — в UI Flowise
- Рабочее время клиники — в конфиге backend

### Архитектура запросов

```
Пользователь → widget.js → Flowise API (диалог)
                ↓
         widget.js → Backend API (заявки, логи)
                ↓
         Backend → Email/CRM
```

---

## Вопросы для уточнения

1. **Backend:** PHP или Node.js для API endpoints?
2. **Рабочее время:** Какие дни и часы работы клиники?
3. **CRM:** Использует ли клиника CRM? Какая?
4. **Email:** На какой адрес отправлять заявки?
5. **Дизайн:** Есть ли брендбук или цветовая схема клиники?

---

## Приоритизация

**Сначала (MVP):**
- P0 задачи из Этапа 1

**Потом:**
- P1 задачи из Этапов 2 и 3

**В конце:**
- P2 задачи из Этапа 4

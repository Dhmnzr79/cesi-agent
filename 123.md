Инцидент: заявки из виджета не отправлялись (Failed to fetch)
Краткое резюме

Заявки из чат-виджета не отправлялись, несмотря на то что:

backend работал,

письма отправлялись при тесте через curl,

логика бота и leadIntent отрабатывали корректно.

Причина была не в коде виджета и не в Flowise, а в конфигурации nginx:

дублирующиеся server-блоки для одного домена,

отсутствие корректного CORS + обработки OPTIONS для POST endpoint’а /lead/send-lead.

В результате браузер блокировал POST-запрос до отправки, что проявлялось как TypeError: Failed to fetch.

Архитектура (контекст)

Виджет подключается на внешних сайтах через:

<script src="https://bot.jeeptour41.ru/widget/widget.js"></script>


Виджет выполняется в контексте сайта-клиента (другой домен).

Запросы:

Flowise API → https://bot.jeeptour41.ru/api/v1/prediction/...

Backend заявок → https://bot.jeeptour41.ru/lead/send-lead

Backend заявок — отдельный сервис (Express, порт 3001).

nginx используется как reverse proxy.

Симптомы
В браузере

Ошибка:

TypeError: Failed to fetch


В Network:

запрос send-lead есть,

размер 0.0 kB,

нет HTTP-статуса,

нет OPTIONS,

нет ответа.

В Console:

нет ошибок CSP,

нет CORS-сообщений.

На сервере

В nginx access.log нет запросов /lead/send-lead.

В логах backend нет входящих запросов.

При этом curl с сервера:

curl -X POST https://bot.jeeptour41.ru/lead/send-lead


→ работал корректно.

Почему это происходило
1. Дублирующиеся server-блоки в nginx

В /etc/nginx/sites-enabled одновременно существовали:

bot.jeeptour41.ru

bot.jeeptour41.ru.bak

Оба содержали server_name bot.jeeptour41.ru.

nginx при старте выдавал предупреждение:

conflicting server name "bot.jeeptour41.ru", ignored


В результате:

nginx игнорировал часть конфигурации,

запросы могли попадать в другой server-блок,

поведение было нестабильным и неочевидным.

2. Отсутствие CORS + preflight для /lead/send-lead

Endpoint /lead/send-lead:

принимает POST с Content-Type: application/json,

вызывается из браузера с другого домена.

Браузер автоматически выполняет preflight OPTIONS.

До исправления:

nginx не обрабатывал OPTIONS,

не отдавал Access-Control-Allow-* заголовки,

браузер прерывал запрос ДО отправки,

fetch падал с Failed to fetch,

запрос не доходил ни до nginx, ни до backend.

Ключевой диагностический признак

Если в DevTools → Network:

есть запрос,

размер 0.0 kB,

нет HTTP-статуса,

нет записи в nginx access.log,

👉 это означает браузер оборвал запрос до HTTP-уровня
(обычно CORS / preflight / неправильный server-блок).

Решение
Шаг 1. Убрать дубли server-блоков

Оставить только один конфиг для домена:

rm /etc/nginx/sites-enabled/bot.jeeptour41.ru.bak


Проверить:

grep -R "server_name bot.jeeptour41.ru" /etc/nginx/sites-enabled

Шаг 2. Добавить CORS + OPTIONS для /lead/send-lead

В рабочем server-блоке nginx:

location /lead/send-lead {

    # CORS для публичного виджета
    add_header Access-Control-Allow-Origin "*" always;
    add_header Access-Control-Allow-Methods "POST, OPTIONS" always;
    add_header Access-Control-Allow-Headers "Content-Type" always;

    # Preflight
    if ($request_method = OPTIONS) {
        return 204;
    }

    proxy_pass http://127.0.0.1:3001/api/send-lead;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}

Шаг 3. Проверка
nginx -t
systemctl reload nginx


Контроль:

curl -i -X OPTIONS https://bot.jeeptour41.ru/lead/send-lead


Ожидаемый ответ:

204 No Content
Access-Control-Allow-Origin: *
Access-Control-Allow-Methods: POST, OPTIONS
Access-Control-Allow-Headers: Content-Type

Итог

После исправлений:

браузер выполняет OPTIONS,

nginx корректно отвечает,

POST уходит на backend,

backend принимает заявку,

письмо отправляется,

fetch больше не падает,

Failed to fetch исчезает.

Важные правила на будущее (чек-лист)
Для любого публичного виджета

Всегда настраивать CORS + OPTIONS для POST endpoint’ов.

Никогда не держать несколько server-блоков с одним server_name.

Проверять не только curl, но и:

DevTools → Network,

nginx access.log.

Если fetch:

без статуса,

без логов,
→ проблема до backend.

Для Cursor / автоматизации

При добавлении нового endpoint:

сразу добавлять CORS,

сразу добавлять OPTIONS,

сразу проверять отсутствие конфликтов server-блоков.

Никогда не считать Failed to fetch «ошибкой фронта» без проверки nginx.